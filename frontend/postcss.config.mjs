/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    // CHANGED BACK: using the standard stable plugin name
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;